import React from 'react';import StartScreen from '@/modules/home-page/start-screen';import ChatWindow from '@/modules/home-page/chat-window';import { Component } from '@/modules/home-page/home-page.styles';import {  useBGSpace,  useSystemActivator,} from '@/modules/home-page/home-page.hooks';function HomePageBody() {  const { systemIsActivated, onClickPowerButton, jsStackIsFinished } =    useSystemActivator();  const { bgSpaceSceneRef } = useBGSpace();  return (    <Component.Main ref={bgSpaceSceneRef}>      {jsStackIsFinished ? (        <React.Fragment>          {!systemIsActivated && (            <StartScreen onClickButton={onClickPowerButton} />          )}          {systemIsActivated && <ChatWindow />}        </React.Fragment>      ) : null}    </Component.Main>  );}export default HomePageBody;